export enum MainPages {
  Auth = 'auth',
  Calendar = 'calendar',
  Statistics = 'statistics',
  Admin = 'admin',
}

export enum AuthPages {
  Login = 'login',
  SignUp = 'sign-up',
}

export enum CalendarPages {
  Calendar = '',
}
