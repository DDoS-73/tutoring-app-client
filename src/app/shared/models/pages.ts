export enum MainPages {
  Auth = 'auth',
  Calendar = 'calendar',
  Statistics = 'statistics',
}

export enum AuthPages {
  Login = 'login',
  SignUp = 'sign-up',
}

export enum CalendarPages {
  Calendar = '',
}
